import java.io.File;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.attribute.BasicFileAttributes;
import java.nio.file.attribute.FileTime;
import java.text.SimpleDateFormat;
import java.util.Date;

public class Task_3 {
    public static void main(String[] args) throws Exception {

        // определяем объект для каталога
        SimpleDateFormat formater = new SimpleDateFormat("yyyy-MM-dd");
        Date d = new Date();
        String path1 = "D:\\testri\\homework_java_2";
        File dir = new File(path1);
        File file = new File("D:\\testri\\homework_java_2\\file1.txt");
        Path filepath = file.toPath();
        BasicFileAttributes attr = Files.readAttributes(filepath, BasicFileAttributes.class);
        System.out.println("creationTime: " + attr.creationTime());
        System.out.println(formater.format(d));
        String dateFile = String.valueOf(attr.creationTime()).substring(0,10);
        Date date = formater.parse(dateFile);
        System.out.println("asdfs");
        System.out.println(formater.format(date));
        System.out.println(formater.format(d));
        if(formater.format(d).equals(formater.format(date))) {
            System.out.println("fghhdfaksjfkj");
        }


        // если объект представляет каталог
        if(dir.isDirectory())
        {
            // получаем все вложенные объекты в каталоге
            for(File item : dir.listFiles()){
                System.out.println(item.getName() + "\t file");
            }
        }
    }
}
